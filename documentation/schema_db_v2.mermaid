erDiagram

    %% ==========================================================================
    %% USERS — App users/
    %% Email-based login. Profile = UI preferences separate from auth.
    %% ==========================================================================

    CustomUser {
        bigint id PK
        string email UK
        string password_hash
        string first_name
        string last_name
        boolean is_active
        boolean is_staff
        boolean is_superuser
        datetime last_login
        datetime date_joined
    }

    Profile {
        bigint id PK
        bigint user_id FK
        string language
        string display_currency
    }

    %% ==========================================================================
    %% ACCOUNTS — App accounts/
    %% Banking infrastructure. AccountAccess REMOVED (YAGNI — 2-user app).
    %% Who uses what = Card.user, not a permissions table.
    %% ==========================================================================

    Bank {
        bigint id PK
        string name UK
        string slug UK
        string country
        string default_currency
        string icon
        boolean is_active
    }

    Account {
        bigint id PK
        bigint bank_id FK
        string name
        string slug UK
        string account_type
        string currency
        boolean is_active
        datetime created_at
    }

    %% Specialised table Phase 0A — checking accounts (Yuh, CIC C/C)
    %% Pattern: 1 Account → 1 specialised table (OneToOne, primary_key=True)
    %% Phase 4+: SavingsAccount, PensionAccount
    %% Phase 5+: InvestmentAccount

    CheckingAccount {
        bigint account_id PK_FK
        string iban
        string bic
    }

    Card {
        bigint id PK
        bigint checking_account_id FK
        bigint user_id FK
        string last_four
        string card_type
        boolean is_active
    }

    BalanceSnapshot {
        bigint id PK
        bigint account_id FK
        date date
        decimal balance
        string currency
        decimal balance_chf
        string source
    }

    ExchangeRate {
        bigint id PK
        date date
        string from_currency
        string to_currency
        decimal rate
    }

    %% ==========================================================================
    %% TRANSACTIONS — App transactions/
    %% ==========================================================================

    Category {
        bigint id PK
        string name UK
        string slug UK
        string icon
        string colour_hex
        int order
        boolean is_system
        boolean is_active
    }

    SubCategory {
        bigint id PK
        bigint category_id FK
        string name
        string slug UK
        string icon
        string default_nature
        boolean is_active
    }

    CategorizationRule {
        bigint id PK
        bigint category_id FK
        bigint subcategory_id FK
        string keyword
        string target_field
        int priority
        boolean is_active
    }

    Transaction {
        bigint id PK
        bigint account_id FK
        bigint card_id FK
        bigint category_id FK
        bigint subcategory_id FK
        bigint categorization_rule_id FK
        bigint paired_transaction_id FK
        date date
        decimal amount
        string currency
        decimal amount_chf
        string description_raw
        string merchant_name
        string note
        string nature
        string categorization_source
        boolean is_reconciled
        boolean is_ignored
        boolean is_recurring
        boolean is_internal_transfer
        string import_hash UK
    }

    ImportLog {
        bigint id PK
        bigint account_id FK
        bigint imported_by_id FK
        string filename
        string file_hash UK
        datetime imported_at
        string status
        int count_created
        int count_skipped
        int count_errors
        string error_detail
    }

    BudgetTarget {
        bigint id PK
        bigint category_id FK
        date period
        decimal amount
    }

    %% BudgetResult: computed live from transactions — no table needed.
    %% Transaction.objects.filter(category=c, date__year=y, date__month=m).aggregate(Sum('amount_chf'))

    %% ==========================================================================
    %% PHASE 4+ — Net worth (not yet implemented)
    %% ==========================================================================

    %% SavingsAccount  { account_id PK_FK, interest_rate, legal_ceiling }
    %% PensionAccount  { account_id PK_FK, pension_type, management_fee }

    %% ==========================================================================
    %% PHASE 5+ — Investments (not yet implemented)
    %% ==========================================================================

    %% InvestmentAccount     { account_id PK_FK, etf_type }
    %% Instrument            { id PK, ticker, name, type, currency, isin }
    %% InvestmentPosition    { id PK, account_id FK, instrument_id FK, ... }
    %% InvestmentTransaction { id PK, account_id FK, instrument_id FK, ... }
    %% PriceHistory          { id PK, instrument_id FK, date, close_price, currency }

    %% ==========================================================================
    %% RELATIONS
    %% ==========================================================================

    %% Users
    CustomUser ||--|| Profile : "has one"

    %% Banks & Accounts
    Bank ||--o{ Account : "hosts"
    Account ||--o| CheckingAccount : "specialised as"
    CheckingAccount ||--o{ Card : "has"
    Account ||--o{ BalanceSnapshot : "balance history"
    Account ||--o{ ImportLog : "tracked by"
    Account ||--o{ Transaction : "contains"

    %% Cards & Users
    CustomUser ||--o{ Card : "cardholder"
    CustomUser ||--o{ ImportLog : "imports"

    %% Transactions
    Card |o--o{ Transaction : "source"
    Category ||--o{ SubCategory : "contains"
    Category ||--o{ Transaction : "classifies"
    Category ||--o{ CategorizationRule : "target"
    Category ||--o{ BudgetTarget : "target"
    SubCategory |o--o{ Transaction : "refines"
    SubCategory ||--o{ CategorizationRule : "refines"
    CategorizationRule |o--o{ Transaction : "categorizes"
    Transaction ||--o| Transaction : "internal transfer link"
