{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "preDeployCommand": [
      "cd src && poetry run python manage.py migrate && poetry run python manage.py sync_reference_data"
    ]
  }
}
